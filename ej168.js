// Crear un documento con el nombre ej168.js
// Lee el siguiente código eintenta desifrar qué resultado da al ejecutar esta función.
// Como parte del ejercicio escribe lo que pasa.


function test() {           //declaramos la funcion test
  if (true) {               //si test es verdadero
    let numero1 = 5;        //declaramos la variable numero1 con un valor de 5
    const numero2 = 10;     //declaramos la constante numero2 con un valor de 10
                            //como no hay comparacion de que es true, no imprime nada?
}
  console.log(numero1);     //estamos imprimiendo el contenido de la variable numero1
  console.log(numero2);     //estamos imprimiendo el contenido de la variable numero2
}

test(); //ejecutamos la funcion test 